flask
flask-cors
boto3
xlsxwriter
openpyxl
requests
Pillow
gunicorn
gevent
# gunicorn's gevent worker imports packaging.version at boot (newer gunicorn
# releases stopped vendoring it) — without this line the service builds fine
# and then dies on startup with ModuleNotFoundError: packaging (Aug 2026).
packaging
# Official SDK for the /api/ai-agent tool loop (the old /api/ai-proxy
# passthrough still uses raw requests and stays untouched).
anthropic
reportlab
pymupdf
opencv-python-headless
# Admin-only P&L (pnl_store.py): every P&L object is encrypted in-process with
# MultiFernet before it reaches S3. Pinned to the version tested locally. If it
# is missing, the P&L answers 503 and the rest of the API boots normally.
cryptography==50.0.1
